# User files

This directory contains files uploaded by users.
Each user has a dedicated subdirectory named after their unique user ID. Inside each user's directory, files are organized into further subdirectories based on file types or categories.